// app/(editor)/site/[siteId]/page/[pageId]/layout.tsx
import type { ReactNode } from "react";

export default function EditorPageLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

